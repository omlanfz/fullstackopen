## Exercise 0.6: New note in Single page app diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User submits form. JS intercepts event, adds new note to local array, clears input field, and re-renders notes list immediately in DOM

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (JSON: {"content": "SPA note", "date": "2026-07-23"})
    activate server
    server-->>browser: HTTP 201 Created {"message": "note created"}
    deactivate server
```